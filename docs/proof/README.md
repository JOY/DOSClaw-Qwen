# Proof Artifacts

This directory is for generated evidence files used during submission packaging.

Generated files such as `*-latest.json` and `dosclaw-qwen-submission-evidence.zip` are local artifacts. Review them before sharing publicly, because they may contain timestamps, public URLs, or cloud error codes from the current deployment attempt.
