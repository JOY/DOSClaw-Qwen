# AgentScope 2.0. Long-term memory uses the Mem0Middleware from upstream PR #1775.
# Until #1775 merges, install agentscope FROM THE PR BRANCH instead of the plain line below:
#   pip install "agentscope[mem0] @ git+https://github.com/Osier-Yi/agentscope.git@mem0-dev"
agentscope>=2.0.1
openai>=1.40.0
mem0ai>=2.0.0,<3.0.0
asyncpg>=0.30.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.1
pytest>=8.0.0
pytest-asyncio>=0.24.0
